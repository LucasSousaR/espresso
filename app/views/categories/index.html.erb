<%= breadcrumb true %>

<%= index_search_form_for @q do |f| %>
  <%= f.input :id_cont, required: false, as: :string %>
<% end %>
<br>
<%= index_table_for(@items) do |t|
    t.column :id
    t.column :name, 'Nome' do |i|
      i.name
    end
    t.column :company_id, 'Empresas' do |i|
      Company.find(i.company_id).name
    end

  end
%>